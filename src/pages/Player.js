import React from 'react';
import Camera from '../components/Camera';

const Player = () => {
    return (
        <div>
            <h1>Player</h1>
            <Camera/>
        </div>
    );
};

export default Player;